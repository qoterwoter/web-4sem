use('students');

// На 5
// агрегация из двух коллекций
db.lessons.aggregate([
    {
        $lookup: {
        from: 'students',
        localField: '_id',
        foreignField: 'lesson1',
        as: 'Ученики',
        }
    }
]).explain()
